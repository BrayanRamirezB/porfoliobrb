---
const { class: className, id } = Astro.props;
---

<section 
    class={`section ${className} scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-3xl sm:max-w-xl`} 
    data-section={id}
    id={id}>	
	<slot />
</section>

